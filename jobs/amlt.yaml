description: mmvmae-dataloader-benchmark

target:
  service: aml
  # name: F32s-v2-ded
  name: V100-1x-ded

environment:
  registry: commondockerimages.azurecr.io
  username: commondockerimages
  image: mm-vmae:latest
  setup:
    - pip install opencv-python
    - pip install mlflow azureml-mlflow
    - pip install -e .

# $CONFIG_DIR is expanded to the directory of this config file.
code:
  local_dir: $CONFIG_DIR/../

storage:
  data:
    storage_account_name: commondatasets2
    container_name: commondatasets
    mount_dir: /mnt/data
    mount_options: ["--file-cache-timeout-in-seconds=0"]
  custom_output:
    storage_account_name: ramadaanaml
    container_name: mmvmae-benchmark-results
    mount_dir: /mnt/data_output
    is_output: True

search:
  job_template:
    # name: job_{experiment_name:s}_{auto:10s}
    name: job_{auto:10s}
    sku: G1
    command:
      - python src/benchmark_dataloader.py
        --train_ann_file /mnt/data/tartanair-release1/train_ann_debug_ratnesh.json
        --benchmark_results_file /mnt/data_output/benchmark_results.csv
        --batch_size {batch_size}
        --workers {num_workers}
        --num_seq {num_seq}
        --seq_len {seq_len}
    submit_args:
      container_args:
        shm_size: 640g
      env: { AZUREML_COMPUTE_USE_COMMON_RUNTIME: True }

  type: grid
  max_trials: 1000
  params:
    - name: num_workers
      values: choice(0, 1, 3, 6)

    - name: batch_size
      values: choice(1, 2, 4, 8, 16, 32, 64)

    - name: num_seq
      values: choice(1, 2, 4, 8)

    - name: seq_len
      values: choice(1, 2, 4, 8, 16)
